# 이 파일을 복사해 .env로 만들고 실제 값을 채워 넣는다. (.env는 절대 커밋 금지)

# 인증
SECRET_KEY=change-this-to-a-long-random-string   # JWT 서명용

# 알림 (채널 정해지면 해당 줄만 채움)
NOTIFY_TARGET=discord     # 또는 telegram
DISCORD_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# DB
DATABASE_URL=sqlite:///./app.db
